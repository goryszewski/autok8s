# generate by ansible
global
        log /dev/log    local0
        log /dev/log    local1 notice
        chroot /var/lib/haproxy
        stats socket /run/haproxy/admin.sock mode 660 level admin expose-fd listeners
        stats timeout 30s
        user haproxy
        group haproxy
        daemon

        # Default SSL material locations
        ca-base /etc/ssl/certs
        crt-base /etc/ssl/private

        # See: https://ssl-config.mozilla.org/#server=haproxy&server-version=2.0.3&config=intermediate
        ssl-default-bind-ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384
        ssl-default-bind-ciphersuites TLS_AES_128_GCM_SHA256:TLS_AES_256_GCM_SHA384:TLS_CHACHA20_POLY1305_SHA256
        ssl-default-bind-options ssl-min-ver TLSv1.2 no-tls-tickets

defaults
        log     global
        mode    tcp


frontend public_{{http}}
        bind *:80
        default_backend web-backend_{{http}}

backend web-backend_{{http}}
        balance roundrobin
{% for worker in groups['worker'] %}
        server worker-{{worker}} {{worker}}:{{http}} check 
{% endfor %}

frontend public_{{https}}
        bind *:443
        default_backend web-backend_{{https}}

backend web-backend_{{https}}
        balance roundrobin
{% for worker in groups['worker'] %}
        server worker-{{worker}} {{worker}}:{{https}} check 
{% endfor %}

frontend public_api
        bind *:6443
        default_backend web-backend_api

backend web-backend_api
        balance roundrobin
{% for master in groups['controlplane'] %}
        server master-{{master}} {{master}}:6443 check 
{% endfor %}
